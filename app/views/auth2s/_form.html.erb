<%= form_for(@auth2) do |f| %>
  <% if @auth2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@auth2.errors.count, "error") %> prohibited this auth2 from being saved:</h2>

      <ul>
      <% @auth2.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :otp %><br>
    <%= f.text_field :otp %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
